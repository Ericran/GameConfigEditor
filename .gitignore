# build artifacts
*.wasm
frontend/dist/*
!frontend/dist/.gitkeep

# dependencies / caches
frontend/node_modules
# package-lock.json IS tracked on purpose - build.sh installs with `npm ci` so a
# given commit always builds against the same dependency versions.
/.sdk/
/.cache/
go.sum.tmp
